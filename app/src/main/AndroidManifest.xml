<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ICapture"
        tools:targetApi="31">
        <activity
            android:name=".Camara"
            android:exported="false"
            android:label="@string/title_activity_camara"
            android:theme="@style/Theme.ICapture" />
        <activity
            android:name=".CambiarContrasenia"
            android:exported="false"
            android:label="@string/title_activity_cambiar_contrasenia"
            android:theme="@style/Theme.ICapture" />
        <activity
            android:name=".CambiarCorreo"
            android:exported="false"
            android:label="@string/title_activity_cambiar_correo"
            android:theme="@style/Theme.ICapture" />
        <activity
            android:name=".Configuracion"
            android:exported="false"
            android:label="@string/title_activity_configuracion"
            android:theme="@style/Theme.ICapture" />
        <activity
            android:name=".Galeria"
            android:exported="false"
            android:label="@string/title_activity_galeria"
            android:theme="@style/Theme.ICapture" />
        <activity
            android:name=".PanelProyectos"
            android:exported="false"
            android:label="@string/title_activity_panel_proyectos"
            android:theme="@style/Theme.ICapture" />
        <activity
            android:name=".RegistrarUsuario"
            android:exported="false"
            android:label="@string/title_activity_panel_proyectos"
            android:theme="@style/Theme.ICapture" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ICapture">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>